# M0—M5开发资料已完成整合

> 开发实施入口：[《八字关系分析系统 M0—M5 详细开发文档 V1.0》](./八字关系分析系统_详细开发文档_V1.0.md)
>
> 当前验收证据：[《S0—S6 完成度审计》](./development/S0-S6-completion-audit.md)
>
> 用户界面与本地运行：[《用户界面与本地开发说明》](./development/UI-and-local-development.md)

## 当前仓库开发入口

运行环境为 Node.js 22.13—25.x。安装后可直接同时启动 Vue 界面与 API：

```bash
npm ci
npm run dev
```

打开 `http://127.0.0.1:5173`。生产同源路径使用 `npm run preview`，然后打开 `http://127.0.0.1:3000`。

公开静态版位于 [https://atong9.github.io/bazi/](https://atong9.github.io/bazi/)。执行 `npm run check:pages` 可在本地重建并校验 Pages 产物；它在浏览器内加载经过摘要校验的 10,918 条运行记录，不依赖远程分析 API。首次加载完成后，同版本界面和规则包会作为一套原子离线缓存保留。

完整校验使用：

```bash
npm run check
```

正式发布执行 `npm run release:check`。真实案例校准保留为可选能力，不再作为工程发布前置条件。

`npm run check` 会执行后端与前端严格类型检查、全部自动化测试、Vue 生产构建、161 文件文档包的清单/哈希/JSON/CSV/ZIP CRC 校验，以及权威来源锁校验。截至 2026-08-30，通用 runner 为 Node 核心 81 个用例加 Web 43 个用例；Pages 另有 5 个专项测试，407 项权威开发矩阵由 `npm run test:evidence` 另行严格执行。各类计数用途不同。原始整合包只读；所有勘误通过 `data/overlays/` 的版本化覆盖层表达。关键契约位于：

- `data/source-package.lock.json`：明确锁定运行时核心、语义权威源和覆盖层；
- `data/migrations/integration-v1.0-canonical-mappings.json`：状态、置信、Excel 日期和夫妻星策略裁决；
- `docs/architecture/adr/`：已接受的架构决策；
- `.github/workflows/validate.yml`：PR 与 master 的统一验证门禁。
- `.github/workflows/deploy-pages.yml`：master 通过全量门禁后构建并发布 GitHub Pages。

重新生成来源锁后必须立即运行校验：

```bash
npm run catalog:lock:build
npm run catalog:lock:verify
```

构建并只读自检当前 Catalog Snapshot：

```bash
npm run catalog:m0:verify
npm run catalog:build
```

快照写入被 Git 忽略的 `rulesets/<digest>/`。运行时只打开其中的 `runtime.sqlite`，不得回退读取 `docs` 下的 CSV 或 XLSX。

已经对两份M0资料包进行解压、识别、字段抽取、版本判定，并与当前页面中的M1—M5文件完成统一整合。

## 完整交付包

[打开《八字关系分析系统 M0-M5 开发整合包 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/)

校验结果：

* 压缩包约 **43.58 MB**
* 包内共 **161个文件**
* ZIP完整性检查通过
* 统一JSON Schema通过Draft 2020-12结构校验
* 核心Excel公式错误扫描未发现明显错误
* 所有源文件均保留SHA-256校验信息

---

# 一、整合后的系统规模

| 模型         |     当前版本 |    记录／规则数 |
| ---------- | -------: | --------: |
| M0 原局结构引擎  |     V1.9 |      1745 |
| M1 心动偏好    |     V0.2 |        94 |
| M2 关系选择    |     V0.8 |      1109 |
| M3 相处惯性    | V0.6部分完成 |      1202 |
| M4 关系盲点    |     V0.9 |      3040 |
| M5 关系适配    |     V0.9 |      3928 |
| **统一记录总数** |     V1.0 | **11118** |

表中的版本与“部分完成”描述来自 Integration V1.0 原始资料，表示源内容成熟度，不是当前代码实现状态；工程完成状态见本文第十节和 S0—S6 审计。

其中：

```text
普通运行时可加载记录：10918
M0测试案例：150
M0版本与争议治理记录：50
M0正式输出字段：45
M0术语与编码：621
```

M0中已经确认：

* `M0_八字结构判定与用神规则库_V1.9_版本与争议治理闭环版.xlsx`为当前M0权威版本；
* M02—M18负责基础计算与综合裁决；
* M19负责45项统一结构输出；
* M20是测试案例，不进入普通运行时；
* M21是版本与争议治理，不进入普通规则执行队列。

---

# 二、开发时先看这三个文件

### 1. 开发入口

[查看《README 先从这里开始》](./八字关系分析系统_M0-M5开发整合包_V1.0/00_开发入口/README_先从这里开始.md)

### 2. 开发实施顺序

[查看《开发实施顺序 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/00_开发入口/开发实施顺序_V1.0.md)

已经把开发过程拆成：

```text
基础数据对象和Schema
→
M0基础识别与关系效应
→
M0旺衰、流通、格局、病药和用神
→
M1心动
→
M2准入
→
补齐M3
→
M4盲点
→
M5适配
→
API与报告
→
回归测试与案例校准
```

### 3. 系统版本清单

[查看《系统版本清单 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/00_开发入口/系统版本清单_V1.0.json)

其中明确记录了：

* 每个模型的当前版本；
* 规则数量；
* 运行时权威文件；
* 当前依赖缺口；
* 系统硬边界；
* 原始文件归档位置。

---

# 三、两份核心开发规范

## 1. M0—M5开发总规范

[下载《八字关系分析系统 M0-M5开发总规范 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/01_系统规范/八字关系分析系统_M0-M5开发总规范_V1.0.md)

规范中已经统一：

* M0—M5总体职责；
* 11118条记录怎样分层；
* 权威源文件如何选择；
* M0四层推理架构；
* 模型调用顺序；
* 单命盘、具体对象、双命盘三种模式；
* 字段权威；
* 同源去重；
* 停止与降级；
* 数据库结构；
* 推荐代码目录；
* 推荐开发顺序；
* 当前系统缺口。

## 2. 模块接口契约

[下载《八字关系分析系统 M0-M5模块接口契约 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/01_系统规范/八字关系分析系统_M0-M5模块接口契约_V1.0.md)

接口契约已经规定：

```text
每个模块允许读取什么
每个模块负责计算什么
每个模块禁止覆盖什么
资料不足时怎样返回
基础争议时怎样停止
安全风险时怎样停止
模块缺失时怎样标记dependency_pending
```

并提供了建议API：

```text
POST /v1/m0/analyze
POST /v1/relationship/profile
POST /v1/relationship/evaluate
POST /v1/calibration/case
```

这是原始整合包的建议列表。当前公开端点已实现前三项，并另有 `GET /health`、`GET /v1/system/manifest`；`POST /v1/calibration/case` 仍未开放，真实案例录入也不在当前 UI 范围。

---

# 四、统一全量规则主库

[下载《八字关系分析系统 M0-M5全量规则主库 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/八字关系分析系统_M0-M5全量规则主库_V1.0.xlsx)

工作簿包含13个工作表：

| 工作表         | 内容             |
| ----------- | -------------- |
| `00_开发导航`   | 开发使用顺序         |
| `01_系统统计`   | 规则数和记录类别       |
| `02_模块注册表`  | 50个统一系统模块      |
| `03_运行时调用链` | M0—M5编排顺序      |
| `04_字段权威`   | 核心字段由哪个模块负责    |
| `05_统一功能族`  | FAM01—FAM10映射  |
| `06_停止降级`   | 输入、安全、RG和证据门控  |
| `07_版本状态`   | 当前版本与依赖缺口      |
| `08_源文件清单`  | 全部源文件及哈希       |
| `09_全量规则`   | 11118条统一规则核心内容 |
| `10_M0输出字段` | M19的45项输出字段    |
| `11_M0术语编码` | 621条原生编码       |
| `12_开发入口`   | 机器文件加载顺序       |

Excel中的`09_全量规则`保留：

```text
global_id
model
module_id
record_class
rule_name
main_signal
required_conditions
excluded_conditions
output_slot
internal_logic
user_explanation
validation_status
source_file
```

为了避免工作簿过度膨胀，完整原生字段另存于机器CSV，没有删除。

---

# 五、机器运行时数据

## 1. M0标准化记录

[下载《M0标准化记录 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/M0_标准化记录_V1.0.csv)

包含M0全部1745条正式记录，并统一为36个开发字段。

记录类型包括：

```text
reference_data
executable_rule
output_contract
test_case
governance_record
```

其中普通运行时只加载：

* M02基础事实；
* M03—M18计算规则；
* M19输出合同。

## 2. M1—M5原子规则总表

[下载《M1-M5原子规则总表 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/M1-M5_原子规则总表_V1.0.csv)

包含9373条M1—M5规则。

已完成M1的接口兼容处理：

```text
M1原来为26字段
M2—M5为31字段
```

M1缺少的五个字段被补为空值：

```text
self_strength_status
spouse_star_status
palace_signal
flow_path
distance_level
```

没有使用推测内容补齐。

## 3. 全局紧凑索引

[下载《M0-M5全量记录索引 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/M0-M5_全量记录索引_V1.0.csv)

适合用于：

* 数据库初始化；
* 全文检索；
* 按模型、模块、字段和状态筛选；
* 快速定位原生文件和行号。

## 4. 模块注册表

[下载《统一模块注册表 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/统一模块注册表_V1.0.csv)

共登记50个系统模块，包括：

```text
M0.SYSTEM
M0.CODEBOOK
M0.M02—M0.M21
M1.CORE
M2七个模块
M3五个已完成模块
M4八个模块
M5八个模块
```

## 5. M0术语编码

[下载《M0术语编码 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/M0_术语编码_V1.0.csv)

包含621条：

* 枚举编码；
* 中文名称；
* 标准定义；
* 适用字段；
* 排序；
* 是否启用；
* 备注。

## 6. M0输出字段

[下载《M0 M19输出字段 V1.0 CSV》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/M0_M19输出字段_V1.0.csv)

包含M0必须生成的45项输出，包括：

* 输入完整度；
* 四柱与月令；
* 根气与透干；
* 干支关系及实际效应；
* 五行有效旺衰；
* 日主旺衰；
* 十神功能；
* 十神组合；
* 流通与卡点；
* 寒暖燥湿；
* 格局成败；
* 病药模型；
* 喜忌用神；
* 最终结构摘要。

---

# 六、统一字段字典与JSON

## 1. 统一字段字典

[下载《八字关系分析系统 M0-M5统一字段字典 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/八字关系分析系统_M0-M5统一字段字典_V1.0.xlsx)

包含：

* 核心API字段；
* M0的45项输出字段；
* M1—M5的31个规则字段；
* 36个标准化记录字段；
* 956类上层输出槽位；
* 字段权威；
* 枚举与状态；
* ID与版本规范；
* 空值与降级规则；
* JSON路径。

## 2. 统一JSON Schema

[下载《八字关系分析系统 M0-M5统一JSON Schema V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/02_运行时核心/八字关系分析系统_M0-M5统一JSON_Schema_V1.0.json)

顶层结构为：

```text
schema_version
system_version
request_context
subjects
m0
m1
m2
m3
m4
m5
evidence
provenance
decision_log
report
boundaries
legacy_payloads
```

Schema已经锁定：

```text
不输出具体事件时点
不输出医学诊断
不替用户作关系决定
unknown不等于负面证据
```

---

# 七、版本迁移与冲突治理

[下载《八字关系分析系统 M0-M5版本迁移与冲突清单 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/04_迁移与治理/八字关系分析系统_M0-M5版本迁移与冲突清单_V1.0.xlsx)

已经识别并处理35类整合问题，包括：

* M0与上层重复计算旺衰；
* 刑冲合会识别直接等于力量效应；
* 上层重新判断合化；
* 心动直接等于适配；
* 准入通过直接等于长期成熟；
* M2确认节奏与M5长期节奏混用；
* M3个人惯性直接等于双人关系；
* M4风险直接等于现实伤害；
* 同一source_id跨模块重复加权；
* 同一event_id被拆成多份现实证据；
* M1的26字段与其余31字段不兼容；
* M5两个校准工作簿的版本重复；
* 历史设计文档覆盖最新版本；
* 模块XLSX和CSV被同时当成机器真源；
* 适配等级被解释为成功概率；
* 病药模型被误解为医学建议；
* 具体对象命盘替代现实行为；
* 传统夫妻星口径缺少配置策略。

同时提供：

* 权威文件选择；
* 字段迁移表；
* ID迁移；
* 状态与置信度映射；
* 重复文件与归档；
* 依赖缺口；
* 运行时记录分类；
* 完整源文件哈希。

---

# 八、开发测试矩阵

[下载《八字关系分析系统 M0-M5开发测试矩阵 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/03_测试与校准/八字关系分析系统_M0-M5开发测试矩阵_V1.0.xlsx)

共定义 **407项测试**：

| 测试套件           |      数量 |
| -------------- | ------: |
| M0原生M20测试      |     150 |
| M0单元与分层测试      |      42 |
| M0—M5接口契约      |      30 |
| M1—M5模块正向／边界测试 |      58 |
| 跨模型冲突与安全测试     |      35 |
| JSON与数据测试      |      25 |
| 报告与语言测试        |      20 |
| 性能与稳定性测试       |      15 |
| M5总合成回归测试      |      32 |
| **合计**         | **407** |

原始工作簿另外提供 200 个执行记录位和自动看板，这是源资料的历史模板容量，不是当前 runner 上限。当前 `test:evidence` 会为全部 407 项生成不截断的 JSONL、JUnit 和摘要，当前结果为 407 `passed`、0 `failed`、0 `review_required`、0 `not_run`。

---

# 九、原始资料的处理方式

本次没有删除或覆盖任何源文件。

整合包中保留：

```text
M0完整归档：35个文件
M1—M5当前页面正式文件：98个
原始上传ZIP：2个
```

统一分为：

```text
authoritative
机器或人工当前权威

mirror
人工阅读镜像，不重复机器加载

reference
速查手册和辅助说明

calibration
案例、反馈和阈值校准资产

historical
旧版本和旧设计文档

internal
预览、渲染和中间文件

deprecated_duplicate
已被后续同版本文件替代
```

完整文件清单：

[下载《文件清单 V1.0》](./八字关系分析系统_M0-M5开发整合包_V1.0/00_开发入口/文件清单_V1.0.csv)

---

# 十、整合阶段遗留项与当前状态

本节原先记录的是整合资料在 2026-08-29 暴露的开发优先级，不再代表当前代码缺口。当前关闭状态以 [S0—S6 完成度审计](./development/S0-S6-completion-audit.md) 和可执行命令为准。

| 整合阶段事项 | 当前工程状态 | 代码边界 |
|---|---|---|
| M3 缺少 REPAIR、STATE、SYNTH | 已关闭 | `packages/relationship-engine/` 已实现并由 M1—M5、接口和 407 矩阵验证 |
| M0 尚无代码 | 已关闭 | M02—M19 已实现；M19 稳定输出 45 项字段 |
| M1—M5 尚未接入 M0 | 已关闭 | `packages/application/` 固定按 M0→M5 编排并保留字段权威、依赖和停止状态 |
| D0 动态岁运与具体时点 | 明确不在当前范围 | 当前请求相关能力返回 `E_DYNAMIC_MODEL_REQUIRED` |

当前 M0 调用顺序仍是：

```text
M02 → M03 → M04/M05 → M06 → M07 → M08 → M09 → M10 → M11
→ M12 → M13 → M14 → M15 → M16 → M17 → M18 → M19
```

这一顺序是运行时依赖约束，不是待实施清单。M1—M5 必须读取已发布的上游结果，继续禁止重复计算或覆盖 M0 权威字段。

## 仍保留的独立范围：D0

当前系统M0—M5都是原局结构与关系模型，不负责：

* 大运；
* 流年；
* 具体相遇年份；
* 婚期；
* 分手或复合日期；
* 事件月份。

这些内容统一留给尚未建设的：

```text
D0 动态岁运与事件时点模型
```

当前工程开发入口已经是 `npm run dev`；后续工作应在现有 M0—M5、API、Vue 界面和测试门禁上继续，不再重新搭建代码骨架。
